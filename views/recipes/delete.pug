extends ../layout

block content
		h2(class="deleteItem") Delete recipe
		p(class="deleteItem") Are you sure you want to delete this recipe?
		form(class="delete deleteItem" action=`/recipes/${recipe.id}/delete`, method="post")
			input(type="submit", value = "Yup!")