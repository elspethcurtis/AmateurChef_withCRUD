extends ../layout

block content
    h2 Delete recipe
    p Are you sure you want to delete this recipe?
    form(class='delete', action=`/recipes/${recipe.id}/delete`, method="post")
        a(href=`/recipes/${recipe.id}`) Nope! Let's go back. 
        input(type="submit", value = "Yup!")